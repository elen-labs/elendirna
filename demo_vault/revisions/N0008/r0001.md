---
baseline: N0008@r0000
created: 2026-04-09T10:57:51.688788100+00:00
---

## Delta

이 Entry는 Elendirna 개발 과정 중 일어난 AI와의 페어 프로그래밍 세션 기록을 저장하기 위한 메모입니다.

### 세션 요약 (v0.2.3 릴리즈 및 v0.3 브레인스토밍)

1. **v0.2.3 패치 릴리즈 & Git Repo 클리닝**
   - 개발 중이던 `demo_vault` 파일들이 crates 배포 및 git history에 오염되는 문제를 롤백하여 해결.
   - `demo_vault` 폴더는 디스크에서 보존하되 git 이력에서 지웠으며, `--allow-dirty` 속성으로 `v0.2.3`을 배포 처리함.

2. **MCP 서버 Vault Path 해석 로직 정리**
   - `ServeArgs`에서 `ELF_VAULT` 환경변수가 없을 경우, 어떻게 Vault를 찾는지 조사함. 
   - 현재 작동 방식은 CWD 디렉터리를 기준으로 상위(parent)로 거슬러 올라가며 `config.toml`을 스캔하여 Vault Root를 찾음. -> 매우 Git-like한 매끄러운 방식임을 확인함.

3. **`--auto-init` 플래그 제안 문서 채택 (`milestones/proposals`)**
   - 위 2번 상황에서 아예 Vault를 찾지 못할 때, Agent의 원활한 온보딩을 돕기 위해 `elf serve --mcp --auto-init` 플래그를 도입하는 아이디어를 냄.
   - 이 아이디어를 공식화하기 위해 `milestones/proposals/001-mcp-auto-init.md` 라는 스펙 문서를 작성 및 저장.
   - v0.2 의 마일스톤 문건들(PLAN_v0.2, cmd-graph)은 모두 개발이 종료되었다고 간주해 `milestones/done/` 폴더로 이동하여 디렉터리 청소를 완료함.