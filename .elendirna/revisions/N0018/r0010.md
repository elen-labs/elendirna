---
baseline: N0018@r0009
created: 2026-04-13T00:00:00+00:00
---

## Delta

## v0.3 / v0.4 범위 재확정 (2026-04-13)

### 변경 사유

Compact Layout + Global Vault Fallback을 v0.3으로 구현 결정.
경로 구조 변경은 사실상 breaking change이므로 minor 버전 격상(0.2.x → 0.3.0) 적용.
기존 이 entry에 기록된 proposals 및 아이디어는 v0.4 범위로 이동.

### v0.3 구현 항목 (확정, 이 세션에서 구현)

| 항목 | 설명 |
|------|------|
| Compact Layout | `entries/`, `revisions/`, `assets/`를 `.elendirna/` 하위로 이동. filesystem 기반 자동 감지(`data_root()`). |
| `elf migrate` | v1 vault를 v2 compact layout으로 이관하는 서브커맨드 |
| Global Vault Fallback | `~/.elendirna/` 폴백 + `elf init --global` |

### v0.4로 이동된 항목 (기존 v0.3 제안)

| 항목 | 원출처 |
|------|--------|
| IDEA-A: MCP 도구에 `vault` 파라미터 추가 (멀티 vault) | r0009 |
| IDEA-B: `elf sessions` — Past Conversations | r0009 |
| 2-A: Vault Export/Migration Tool (`elf export`, `elf merge`) | note §2-A |
| 2-B: `elf init --local-only` 플래그 | note §2-B |
| 2-C: MCP 서버 설정 자동 주입 | note §2-C |
| 2-D: SSE Transport 지원 | note §2-D |
| 3-C/3-D: CLAUDE.md → MCP 프로토콜 대체 | note §3-C, §3-D |
| `elf doctor`, `elf help --json`, `elf entry show --bundle` | N0004 §v0.3 원안 |

→ see N0004 (ROADMAP) — v0.4 항목으로 반영 필요
