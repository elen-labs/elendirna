---
baseline: N0004@r0002
created: 2026-04-14T00:00:00+00:00
---

## Delta

## v0.4 스코프 최종 확정 + 9·10번 처리 방향 (2026-04-14)

r0002에서 3개로 줄였던 v0.4 스코프를 재조정.

### v0.4 확정 (6개)

| 항목 |
|------|
| MCP 도구 `description` 강화 (트리거 조건 내장) |
| `elf serve --mcp` 미연결 시 설정 snippet 출력 |
| `bundle --since` |
| `bundle --depth N` |
| `elf help --json` (AI Readable) |
| Multi-Vault (`@vault:` 문법 파서, `--vault` CLI/MCP 파라미터) |

`elf entry status` — 별도 재검토 (r0002에서 확정했으나 이번 논의에서 언급 안 됨, 유지 여부 확인 필요)

### 9번 — `elf sessions` 유보

판단 기준: AI가 "우리 이런 주제에 대해 어떤 이야기를 나눴지"에 어떻게 답하느냐.
현재 경로(`sync.jsonl` 읽기 → `bundle` 탐색)로 커버 가능한지 실사용에서 확인 후 결정.
cold start 비용이 실제로 문제가 될 때 재논의.
→ see N0019 r0005 (Recap Entry 패턴과 같은 선결 조건 공유)

### 10번 — SSE Transport 별도 entry로 분리

선결 조건(Multi-Vault)이 있고, `author` 필드 강제 등 manifest 스키마 변경을 동반할 수 있어 독립 논의 필요.
→ see N0033 (SSE Transport & HTTP MCP Server 설계 논의)
